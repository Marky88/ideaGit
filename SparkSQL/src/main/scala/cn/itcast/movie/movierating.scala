package cn.itcast.movie

case class movierating(
                      userid :String,
                      itemid : String,
                      rating : Double,
                      timestamp : Long
                      )
